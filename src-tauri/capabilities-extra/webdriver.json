{
  "identifier": "webdriver-testing",
  "description": "Grants plugin permissions to remote URLs for Tauri WebDriver smoke tests. Only loaded at runtime when the webdriver feature is enabled.",
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "local": false,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:default",
    "dialog:default",
    "opener:default"
  ]
}
