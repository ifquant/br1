// Boundary: this placeholder barrel marks the shared store surface for the
// renderer. Keep future exports renderer-safe, and avoid pulling privileged
// service behavior into store modules by default.

// Reader, library, and bridge state will be added here in later slices.
export {};
